
docker:
  context: default
  identifier: apps

sops:
  age:
    key_file: ${SOPS_AGE_KEY_FILE}

stacks:
  traefik:
    root: apps/traefik
    secrets:
      sops:
        - secrets.traefik.enc.env

  glance:
    root: apps/glance


networks:
  proxy-net:


filesets: {}