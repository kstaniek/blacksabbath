
docker:
  context: default
  identifier: apps

sops:
  age:
    key_file: ${SOPS_AGE_KEY_FILE}

stacks:
  traefik:
    root: apps/traefik
    secrets:
      sops:
        - secrets.traefik.enc.env

  glance:
    root: apps/glance

  netbox:
    root: apps/netbox
    secrets:
      sops:
        - secrets.netbox.enc.env

networks:
  netbox-net:
  proxy-net:


filesets: {}